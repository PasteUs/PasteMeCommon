spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:4399/pasteme
      username: username
      password: password
    schema:
      - classpath:sql/init/*.sql

    initialization-mode: always
